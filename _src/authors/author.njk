---
pagination:
  data: collections.pagedAuthorPosts
  size: 1
  alias: author
permalink: authors/{{ author.slug }}/index.html
eleventyComputed:
  title: "{% set myAuthor = authors | getAuthor(author.title) %}{{ myAuthor.name }}"
pageID: authorPosts
layout: base.njk
---

{% set myAuthor = authors | getAuthor(author.title) %}

<h1>{{ myAuthor.name }}</h1>

<p>
  {{ myAuthor.bio }}
</p>

{% for post in author.items %}
{% include "partials/posts.njk" %}
{% endfor %}

<br>

{% set paged = author.pageSlugs %}
{% set nestedPagination = true %}
{% set authorPagination = true %}
{% include "partials/paginator.njk" %}